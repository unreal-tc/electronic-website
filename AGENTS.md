# AGENTS.md

## Cursor Cloud specific instructions

This repository (`electronic-website` / 电商网站) is currently an empty scaffold — it contains only a `README.md` and an Apache 2.0 `LICENSE`. No source code, build configuration, package manifests, or runnable services exist yet.

### Current state

- **Language/Framework:** Not yet chosen.
- **Package manager:** None configured.
- **Build tools:** None configured.
- **Services:** None to start.
- **Tests:** None to run.
- **Lint:** None configured.

### Notes for future agents

- Once the project is bootstrapped with actual code and dependencies, update this section with specific run/test/lint commands and any non-obvious caveats.
- The workspace rules (`.cursorrules` / always-applied rules) require specific comment conventions for AI-generated code — see the ZKT AI 研发规范 for details on file headers, code-block annotations, and `AI-GENERATED-BEGIN`/`AI-GENERATED-END` markers.
- User rules specify: respond in Chinese, use `python3` for Python commands, and consult `.cursor/skills/` for domain-specific guidance (no skills exist yet).
